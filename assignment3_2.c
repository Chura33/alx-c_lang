/**
 * program that loops through all d numbers from 0-100,
 * even for an even number
 * odd for an odd number.
 * be sure to use a new line
 */

#include <stdio.h>
void  main(void)
{
	int y;
	for (i = 0; i <= 100; i++)
	{
		if (i % 2 == 0)
		{
			print("%d is an even number\n", i);
		}
	}
	{
		if (i % 2 != 0)
		{
			print("%d is an odd number\n", i);
		}
	}
	return (0);
}
